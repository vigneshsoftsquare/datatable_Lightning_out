<!DOCTYPE html>
<html lang="en">
  <head>
  </head>
  <body class="slds">
    <header>
      hello
    </header>
    <main>
      <div >
        <p>Sales Leaderboard On Heroku</p>
        <div id="leaderboard"></div>
          </div>
      </main>
        <footer>
        </footer>
        <!--script>
          var url = "<%= ouathLightningURL %>";
          var token = "Bearer "+"<%= oauthtoken %>";
          console.log(token);
            $Lightning.use("datatable:LdtAppOuttest", function() {
                  $Lightning.createComponent("datatable:LightningDataTableWrapper", {},
                     "leaderboard",
                      function(cmp) {
                         // do some stuff
                      });
            },url,token );
        </script-->
  </body>
</html>